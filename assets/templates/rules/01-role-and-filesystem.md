# Role and filesystem rules

Use the standard role boundaries and filesystem routing from `AGENTS.md`. The default startup role is `monitor`; it requests owner authorization before creating missing standard paths. Employee creation uses the owner-specified model level, otherwise `gpt-5.6-luna` with `high` reasoning. Every employee delivery must request assistant audit. Add only owner-confirmed exceptions below.

<!-- Project-specific exceptions or stricter restrictions. -->
