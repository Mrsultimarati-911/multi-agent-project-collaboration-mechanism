# Work-log governance

`record` is the sole `work_logs/` writer. Logs record auditable events only. Use level 1 for project stages/assistant initiatives and level 2 for employee tasks. Keep `level2_results_mid` append-only; create a final level 2 result only after assistant audit.
